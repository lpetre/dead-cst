[project]
name = "reportkit"
version = "0.0.0"
description = "Library + console-script demo for dead-cst's ProjectScriptsPlugin and ModuleDundersPlugin."
requires-python = ">=3.11"

[project.scripts]
reportkit = "reportkit.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
