[project]
name = "core"
version = "0.0.0"
description = "Library half of the dead-cst uv-workspace demo."
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
