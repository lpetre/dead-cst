[project]
name = "uv-workspace-demo"
version = "0.0.0"
description = "Multi-package uv workspace for exercising dead-cst's -p search-path specs."
requires-python = ">=3.11"

[tool.uv.workspace]
members = ["packages/*"]

[tool.uv.sources]
core = { workspace = true }
app = { workspace = true }
